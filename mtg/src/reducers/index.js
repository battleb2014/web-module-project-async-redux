

const initialState = {
    img: '',
    name: '',
    manaCost: '',
    rarity: '',
    rulings: []
};

export const reducer = ( state = initialState, action ) => {
    switch( action.type ) {
        default:
            return state;
    }
}